{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd charges-fields-lab-app && npm install && npm run build && cd ../charges-and-fields && npm install && ls -la && mkdir -p build && find . -name '*_en.html' -exec cp {} build/ \\;"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  },
  "nixpacks": {
    "features": {
      "node": {
        "npmFlags": ["--legacy-peer-deps", "--omit=dev"]
      }
    }
  }
} 