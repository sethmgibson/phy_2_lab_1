{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd charges-fields-lab-app && npm install && npm run build && cp -v ../charges-and-fields/charges-and-fields_en.html ../charges-and-fields_en.html && ls -la ../"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  },
  "nixpacks": {
    "features": {
      "node": {
        "npmFlags": ["--legacy-peer-deps", "--omit=dev"]
      }
    }
  }
} 